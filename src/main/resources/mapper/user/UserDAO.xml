<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recklessmo.dao.user.UserDAO">
    <sql id = "columns">
        id, user_name, pwd, name, phone
    </sql>

    <resultMap id = "result" type="User">
        <result property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="pwd" column="pwd"/>
        <result property="phone" column="phone"/>
        <result property="name" column="name"/>
    </resultMap>

    <sql id="byPageQuery">
        <if test="searchStr != null and '' != searchStr">
            <where>
            (user_name like "%"#{searchStr}"%" or name like "%"#{searchStr}"%" or phone like "%"#{searchStr}"%")
            </where>
        </if>
    </sql>

    <select id="getAllUser" resultMap = "result">
        select
        <include refid="columns"/>
        from user
        <include refid="byPageQuery"/>
        <if test="page != 0">
            limit #{startIndex}, #{count}
        </if>
    </select>

    <select id="getCount" resultType="java.lang.Integer">
        select count(1) from user
        <include refid="byPageQuery"/>
    </select>

    <select id="getUser" resultMap = "result">
        select
        <include refid="columns"/>
        from user where id = #{id}
    </select>

    <select id="getUserByUserName" resultMap = "result">
        select
        <include refid="columns"/>
        from user where user_name = #{userName}
    </select>

    <insert id="addUser" parameterType = "com.recklessmo.model.user.User">
        insert into user(user_name, pwd, name, phone) values(#{userName}, #{pwd}, #{name}, #{phone})
    </insert>

    <update id="updateUser" parameterType = "com.recklessmo.model.user.User">
        update user set user_name = #{userName}, pwd = #{pwd}, name = #{name}, phone=#{phone} where id = #{id}
    </update>

    <delete id = "deleteUser" parameterType = "java.lang.Long">
        delete from user where id = #{id}
    </delete>

</mapper>