<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recklessmo.dao.wechat.WechatMessageDAO">
    <sql id="columns">
        id,org_id,open_id,user_id,type,message_type,message,created
    </sql>

    <insert id="insertMessage" parameterType="com.recklessmo.model.wechat.WechatMessage">
        insert into wechat_msg(org_id, open_id, user_id, type, message_type, message, created)
        values(#{orgId}, #{openId}, #{userId}, #{type}, #{messageType}, #{message}, #{created})
    </insert>

    <select id="getListByOrgIdAndOpenId" resultType="com.recklessmo.model.wechat.WechatMessage">
        select
        <include refid="columns"/>
        from wechat_msg
        where org_id = #{orgId} and open_id = #{openId}
        limit #{startIndex}, #{count}
    </select>

    <select id="getCountByOrgIdAndOpenId" resultType="java.lang.Integer">
        select count(1)
        from wechat_user
        where org_id = #{orgId} and open_id = #{openId}
    </select>

</mapper>